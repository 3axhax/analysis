services:
  nginx:
    image: nginx:alpine
    container_name: nginx-test
    ports:
      - "80:80"
      - "8080:80"  # альтернативный порт, если 80 занят
      - "443:443"  # альтернативный порт, если 80 занят
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro  # опционально
    restart: unless-stopped
    networks:
      - test-network

networks:
  test-network:
    driver: bridge